apiVersion: v1
kind: Service
metadata:
  name: mlops-serving
  labels:
    app: mlops-serving
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: mlops-serving
